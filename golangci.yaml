version: "2"
linters:
  enable:
    - revive
formatters:
  enable:
    - goimports
    - golines
    - gofumpt
  settings:
    gofumpt:
      extra-rules: true
    goimports:
      local-prefixes:
        - "github.com/nuchs"
    golines:
      max-len: 80
      tab-len: 2
